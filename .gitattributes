# Binary media is stored directly in git (no Git LFS).
# Built Anki packages (*.apkg) are NOT committed — they ship via GitHub Releases.
*.mp3  -text
*.svg  -text
*.jpg  -text
*.png  -text
*.sqlite -text
*.gz   -text
*.bz2  -text
*.zip  -text
